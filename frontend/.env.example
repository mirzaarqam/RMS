# API URL Configuration
# Copy this file to .env and configure for your environment

# Development (Windows local setup - Flask on port 5000)
# REACT_APP_API_URL=http://localhost:5000/api

# Production (Ubuntu server with Nginx reverse proxy on port 8088)
# REACT_APP_API_URL=http://your-server-ip:5000/api
# Or if Nginx is proxying API requests:
# REACT_APP_API_URL=http://your-server-ip:8088/api

# If not set, the app will auto-detect using window.location.hostname:5000
